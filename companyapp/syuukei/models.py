#from django.db import models

## Create your models here.
#class UploadedFile(models.Model):
    #file = models.FileField(upload_to='uploads/')  # アップロードされたファイルを保存するフィールド

#class ProcessedData(models.Model):
    ## プロセスされたデータを保存するフィールドを定義
    ## 例: 担当者コード、担当者略称、直接工数、関接工数、関接率など
    #pass
